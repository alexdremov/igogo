{% set version = "1.1.0" %}


package:
  name: igogo
  version: {{ version }}

source:
  git_rev: {{ version }}
  git_url: https://github.com/AlexRoar/igogo.git

requirements:
  build:
    - python
    - setuptools

  run:
    - python

about:
  home: https://github.com/AlexRoar/igogo
